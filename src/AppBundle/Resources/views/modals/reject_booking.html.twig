<div class="modal fade" id="reject-booking" tabindex="-1" role="dialog" aria-labelledby="signinFormLabel">
    <div class="modal-dialog modal-md" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <a href="#" class="close" data-dismiss="modal" aria-label="Close">
                    <img src="{{ asset('images/error.png') }}" alt="">
                </a>
            </div>

            <div class="modal-body">

                <img src="{{ asset('images/icons/red/cancel.svg') }}" alt="">

                <div class="title">{{ 'artist.edit.booking.reject.title' | trans }}</div>
                <div class="subtitle">{{ 'artist.edit.booking.reject.subtitle' | trans }}</div>

                <button type="button" class="btn-signup rejecting-booking" data-booking-id="{{ id }}">{{ 'artist.edit.booking.reject.button' | trans }}</button>

            </div>
        </div>
    </div>
</div>